{
  "name": "tst",
  "version": "1.0.0",
  "description": "TypeScript test",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc",
    "dev": "nodemon --watch './src/*' -e ts,tsx --exec \"npm run build && node ./dist/index\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsc": "^1.20150623.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "nodemon": "^2.0.3"
  }
}
